<h1>Image <%= @image.id %></h1>
<p id="notice"><%= notice %></p>

<div>
<%= render 'tag_list', image: @image %>
</div>
<div>
  <%= image_tag @image.url, class: 'image-stream' %>
</div>

<%= link_to 'Destroy', @image, method: :delete, data: { confirm: 'Are you sure?' } %>
<%= link_to 'Home', root_path %>
